---
phase: 03-content-case-studies
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements:
  - CASE-03
  - COPY-01

must_haves:
  truths:
    - "Each case study page is visually readable with clear section hierarchy"
    - "Tech tags render as mono-label styled text, not a logo grid"
    - "Links open correctly to external URLs"
    - "Copy reads like a person explaining their work, not a marketing page"
    - "Page is scannable in 90 seconds — hook visible immediately, approach and outcome reachable without searching"
  artifacts: []
  key_links: []
---

<objective>
Verify all four case study pages render correctly, read well, and pass the anti-slop gate.

Purpose: Visual and content verification before moving to Phase 4. Ensures the case studies look intentional and read authentically.
Output: Human-verified case study pages ready for integration into the home page projects section in Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-case-studies/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run anti-slop gate and content verification</name>
  <files></files>
  <read_first>
    src/lib/case-studies.ts
    src/lib/design-constraints.ts
    src/app/work/[slug]/page.tsx
    src/components/case-study/CaseStudyHero.tsx
    src/components/case-study/CaseStudySection.tsx
    src/components/case-study/TechTag.tsx
  </read_first>
  <action>
    Run the full anti-slop checklist verification against all case study files. Check each of the 15 ANTI_SLOP_CHECKLIST items from design-constraints.ts against case study code and content:

    1. Grep for "Inter" in case study files — must find 0 matches
    2. Grep for "gradient" in case study component files — must find 0 matches
    3. Grep for "bento" in case study files — must find 0 matches
    4. Grep for "typewriter" in case study files — must find 0 matches
    5. Grep for "particle|starfield|floating" in case study files — must find 0 matches
    6. Grep for "glassmorphism|frosted" in case study files — must find 0 matches
    7. Grep for "skill-bar|progress-bar" in case study files — must find 0 matches
    8. Grep for "logo-grid|tech-grid" in case study files — must find 0 matches
    9. Grep for "build something amazing|available for opportunities" in case study files — must find 0 matches
    10. Grep for "cursor-trail|cursor-follow" in case study files — must find 0 matches
    11. Verify no "innovative|passionate developer|cutting.edge" in any case study content

    Also verify content completeness:
    - All 4 slugs present in CASE_STUDIES array
    - Each case study has non-empty problem, approach, outcome, tradeoffs
    - Each tradeoffs section has at least 2 bullets
    - VTX has no github link, awesome-mpp has no live link
    - Tech tags use specific library names (grep for Bun, Hono, NestJS, x402)

    If any issues found, fix them in the relevant files. If all pass, report clean.
  </action>
  <verify>
    <automated>cd /Users/vtx/max-portfolio && npm run build && npm run lint && echo "---" && ! grep -riE "innovative|passionate developer|cutting.edge|glassmorphism|gradient|bento|typewriter|particle|starfield|cursor-trail" src/lib/case-studies.ts src/app/work/ src/components/case-study/ && echo "ALL ANTI-SLOP CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - npm run lint exits 0
    - Zero matches for any anti-slop pattern in case study files
    - All 4 routes appear in build output as static pages
  </acceptance_criteria>
  <done>
    Anti-slop gate passed. Build and lint clean. All case study content verified against the 15-item checklist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and content verification of case study pages</name>
  <files></files>
  <action>
    Start the dev server with `npm run dev`. Present the user with instructions to verify all 4 case study pages visually. No code changes needed — this is a human verification checkpoint.
  </action>
  <what-built>
    Four case study pages at /work/tonos, /work/vtx, /work/apimesh, /work/awesome-mpp. Each page has a hero with title, hook, tech tags, and links, followed by problem, approach, outcome, and tradeoffs sections. Content extracted from actual project codebases. Styled using Phase 2 design tokens (Space Grotesk, stone palette, coral accent, mono-label tech tags).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/work/tonos
    2. Check: title shows "tonos", tech tags are visible as small mono-label pills, links to tonos.fyi and GitHub work
    3. Scroll through problem, approach, outcome, tradeoffs — content should be specific (mentions Bun, Hono, Voyage AI, MCP tools)
    4. Visit /work/vtx — check there is NO GitHub link (private repo), live link to vtxathlete.com works
    5. Visit /work/apimesh — check all 3 links present (live, github, npm)
    6. Visit /work/awesome-mpp — check only GitHub link present (no live URL)
    7. On any page: does the copy sound like Max explaining his work, or like an AI marketing page? Should feel direct and specific.
    8. Each page should be scannable in ~90 seconds — hook visible immediately at the top, you can find the approach and outcome without searching
    9. Visit /work/nonexistent — should show 404
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint — no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 4 pages render with correct content
    - User confirms copy reads in Max's voice (not AI marketing)
    - User confirms pages are scannable within 90 seconds
  </acceptance_criteria>
  <done>
    User has approved all 4 case study pages. Phase 3 content is complete and ready for Phase 5 home page integration.
  </done>
  <resume-signal>Type "approved" or describe any issues with layout, content, or voice</resume-signal>
</task>

</tasks>

<verification>
1. Anti-slop gate: all 15 checklist items pass
2. Human verification: all 4 pages render correctly with accurate content
3. 404 works for unknown slugs
4. Copy reads authentically in Max's voice
</verification>

<success_criteria>
- Human confirms all 4 case study pages look good and read authentically
- Anti-slop checklist fully passed
- Build and lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-case-studies/03-02-SUMMARY.md`
</output>
