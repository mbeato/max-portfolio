---
phase: 03-content-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/case-studies.ts
  - src/app/work/[slug]/page.tsx
  - src/components/case-study/CaseStudyHero.tsx
  - src/components/case-study/CaseStudySection.tsx
  - src/components/case-study/TechTag.tsx
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-03
  - CASE-04
  - CASE-05
  - COPY-01
  - COPY-04
  - COPY-05
  - COPY-06
  - COPY-07
  - COPY-08

must_haves:
  truths:
    - "Visiting /work/tonos renders the Tonos case study with real content"
    - "Visiting /work/vtx renders the VTX case study with real content"
    - "Visiting /work/apimesh renders the APIMesh case study with real content"
    - "Visiting /work/awesome-mpp renders the awesome-mpp case study with real content"
    - "Visiting /work/nonexistent returns a 404 page"
    - "Each case study shows specific tech stack names, not generic descriptions"
    - "Each case study has at least one tradeoff or rejected alternative"
    - "All copy is lowercase-first, direct, no buzzwords"
  artifacts:
    - path: "src/lib/case-studies.ts"
      provides: "CaseStudy type + 4 case study data objects + getCaseStudy + getAllSlugs"
      exports: ["CaseStudy", "TechTag", "CaseStudyLink", "CaseStudySection", "CASE_STUDIES", "getCaseStudy", "getAllSlugs"]
    - path: "src/app/work/[slug]/page.tsx"
      provides: "Dynamic route with generateStaticParams for 4 slugs"
      exports: ["generateStaticParams", "generateMetadata", "default"]
    - path: "src/components/case-study/CaseStudyHero.tsx"
      provides: "Hero section for case study pages"
      min_lines: 30
    - path: "src/components/case-study/CaseStudySection.tsx"
      provides: "Reusable section component for problem/approach/outcome/tradeoffs"
      min_lines: 15
    - path: "src/components/case-study/TechTag.tsx"
      provides: "Mono Label tech tag component"
      min_lines: 10
  key_links:
    - from: "src/app/work/[slug]/page.tsx"
      to: "src/lib/case-studies.ts"
      via: "getCaseStudy(slug) lookup"
      pattern: "getCaseStudy\\("
    - from: "src/app/work/[slug]/page.tsx"
      to: "src/components/case-study/CaseStudyHero.tsx"
      via: "import and render"
      pattern: "CaseStudyHero"
    - from: "src/app/work/[slug]/page.tsx"
      to: "src/components/case-study/CaseStudySection.tsx"
      via: "import and render for each section"
      pattern: "CaseStudySection"
---

<objective>
Build the complete case study infrastructure and populate all four case studies with real content extracted from actual project codebases.

Purpose: This is the core content phase — recruiters, collaborators, and clients will read these pages to understand what Max actually built. Every technical claim must trace back to a real codebase.
Output: 4 statically generated case study pages at /work/tonos, /work/vtx, /work/apimesh, /work/awesome-mpp with real content, tech stacks, links, and tradeoffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-case-studies/03-RESEARCH.md

@src/lib/motion.ts
@src/lib/design-constraints.ts
@src/app/globals.css
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case study route, components, and data types</name>
  <files>
    src/lib/case-studies.ts
    src/app/work/[slug]/page.tsx
    src/components/case-study/CaseStudyHero.tsx
    src/components/case-study/CaseStudySection.tsx
    src/components/case-study/TechTag.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/lib/motion.ts
    src/lib/design-constraints.ts
    src/lib/types.ts
    src/app/layout.tsx
    .planning/phases/03-content-case-studies/03-RESEARCH.md
  </read_first>
  <action>
    **1. Create `src/lib/case-studies.ts`** with these exact types and exports:

    ```typescript
    export interface TechTag {
      label: string;
      category: 'runtime' | 'framework' | 'database' | 'protocol' | 'service' | 'tool';
    }

    export interface CaseStudyLink {
      label: string;
      url: string;
      type: 'live' | 'github' | 'npm' | 'docs';
    }

    export interface CaseStudySection {
      heading: string;
      body: string;
      bullets?: string[];
    }

    export interface CaseStudy {
      slug: string;
      title: string;
      hook: string;
      problem: CaseStudySection;
      approach: CaseStudySection;
      outcome: CaseStudySection;
      tradeoffs: CaseStudySection;
      tech: TechTag[];
      links: CaseStudyLink[];
      year?: string;
      status?: 'live' | 'private' | 'open-source';
    }
    ```

    Export `CASE_STUDIES` as an empty array initially (Task 2 populates it).
    Export `getAllSlugs()` returning `CASE_STUDIES.map((s) => s.slug)`.
    Export `getCaseStudy(slug: string): CaseStudy | undefined` returning `CASE_STUDIES.find((s) => s.slug === slug)`.

    **2. Create `src/components/case-study/TechTag.tsx`** — a client component:
    - Renders a `<span>` with the tech label
    - Tailwind classes: `font-mono text-mono-label tracking-[0.3px] uppercase bg-stone-100 text-stone-700 px-3 py-1.5 rounded-subtle`
    - Props: `{ label: string; category?: string }` — category is for future styling hooks, not used visually yet
    - Do NOT use SVG logos or icons — text only (SLOP-09 compliance)

    **3. Create `src/components/case-study/CaseStudySection.tsx`** — a server component:
    - Props: `{ section: CaseStudySection }` imported from `@/lib/case-studies`
    - Renders `<section>` with:
      - `<h2>` using `text-h2 tracking-[-0.8px] leading-[1.25] font-semibold text-contour-black`
      - Body text split by `\n\n` into `<p>` elements, each with `text-body leading-[1.65] text-stone-700 mt-4`
      - If `bullets` exists, render `<ul>` with `list-disc pl-6 mt-4 space-y-2` and each bullet as `<li>` with `text-body leading-[1.65] text-stone-700`
    - Section wrapper: `py-12` vertical padding

    **4. Create `src/components/case-study/CaseStudyHero.tsx`** — a client component (needs Framer Motion):
    - Props: `{ study: CaseStudy }` imported from `@/lib/case-studies`
    - Uses `'use client'` directive
    - Imports `fadeInUp`, `staggerContainer` from `@/lib/motion`
    - Renders inside `motion.div` with `variants={staggerContainer}` and `initial="initial" animate="animate"`:
      - `<h1>` with `text-h1 tracking-[-1.2px] leading-[1.15] font-bold text-contour-black` wrapped in `motion.div` with `variants={fadeInUp}`
      - Hook text as `<p>` with `text-body-lg leading-[1.7] text-stone-700 mt-4` wrapped in `motion.div` with `variants={fadeInUp}`
      - Tech tags: `<div className="flex flex-wrap gap-2 mt-6">` with each `study.tech` rendered as `<TechTag>`, wrapped in `motion.div` with `variants={fadeInUp}`
      - Links: `<div className="flex flex-wrap gap-4 mt-6">` with each `study.links` rendered as `<a>` elements. Use classes: `font-mono text-mono-label uppercase tracking-[0.3px] text-coral-peak hover:text-coral-deep transition-colors` with `target="_blank" rel="noopener noreferrer"` for external links. Wrap in `motion.div` with `variants={fadeInUp}`
    - Show `study.year` and `study.status` if present, as small metadata below the title: `text-label tracking-[0.5px] uppercase text-stone-500`

    **5. Create `src/app/work/[slug]/page.tsx`** — a server component:
    - CRITICAL: Next.js 15 params are Promises. Type as `params: Promise<{ slug: string }>` and await them.
    - Import `notFound` from `next/navigation`
    - Import `getCaseStudy`, `getAllSlugs` from `@/lib/case-studies`
    - Import `CaseStudyHero` from `@/components/case-study/CaseStudyHero`
    - Import `CaseStudySection` from `@/components/case-study/CaseStudySection`
    - Import `type Metadata` from `next`

    `generateStaticParams()`: return `getAllSlugs().map((slug) => ({ slug }))`

    `generateMetadata({ params })`: await params, get study via `getCaseStudy(slug)`, return `{ title: study.title, description: study.hook }`. Return empty object if not found.

    Default export `CaseStudyPage({ params })`:
    - `const { slug } = await params`
    - `const study = getCaseStudy(slug)`
    - `if (!study) notFound()`
    - Return layout: `<main className="max-w-[720px] mx-auto px-6 pt-24 pb-16">`
      - `<CaseStudyHero study={study} />`
      - `<CaseStudySection section={study.problem} />`
      - `<CaseStudySection section={study.approach} />`
      - `<CaseStudySection section={study.outcome} />`
      - `<CaseStudySection section={study.tradeoffs} />`
      - Back link at bottom: `<a href="/" className="inline-block mt-12 font-mono text-mono-label uppercase tracking-[0.3px] text-stone-500 hover:text-contour-black transition-colors">back to home</a>`
  </action>
  <verify>
    <automated>cd /Users/vtx/max-portfolio && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/case-studies.ts contains `export interface CaseStudy`
    - src/lib/case-studies.ts contains `export function getCaseStudy(`
    - src/lib/case-studies.ts contains `export function getAllSlugs(`
    - src/lib/case-studies.ts contains `export const CASE_STUDIES`
    - src/app/work/[slug]/page.tsx contains `generateStaticParams`
    - src/app/work/[slug]/page.tsx contains `await params`
    - src/app/work/[slug]/page.tsx contains `params: Promise<{ slug: string }>`
    - src/app/work/[slug]/page.tsx contains `notFound()`
    - src/components/case-study/TechTag.tsx contains `text-mono-label`
    - src/components/case-study/TechTag.tsx does NOT contain any SVG or logo imports
    - src/components/case-study/CaseStudyHero.tsx contains `'use client'`
    - src/components/case-study/CaseStudyHero.tsx contains `fadeInUp`
    - src/components/case-study/CaseStudySection.tsx contains `CaseStudySection`
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>
    All 5 files exist. Route compiles. Types are exported. Components render case study data using design tokens from globals.css. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate all 4 case studies with real codebase content</name>
  <files>
    src/lib/case-studies.ts
  </files>
  <read_first>
    src/lib/case-studies.ts
    .planning/phases/03-content-case-studies/03-RESEARCH.md
    src/lib/design-constraints.ts
  </read_first>
  <action>
    Populate the `CASE_STUDIES` array in `src/lib/case-studies.ts` with all 4 case study objects. ALL content comes from the "Project Codebase Intelligence" section of 03-RESEARCH.md. Do NOT fabricate any technical claims.

    **Voice rules (COPY-01):** All body text in Max's voice — lowercase sentence starts where natural, direct, no buzzwords. Never write "innovative", "passionate", "robust", "scalable architecture", "modern stack", "cutting-edge", or "let's build something". Write as if Max is explaining his project to a fellow engineer over coffee.

    **Case Study 1: Tonos** (slug: `tonos`)
    - title: `"tonos"`
    - hook: `"a voice profile api that learns how you write and generates text that sounds like you — not like an ai assistant"`
    - year: `"2025"`
    - status: `"live"`
    - problem.heading: `"the problem"`
    - problem.body: Two paragraphs. First: LLMs write in their own voice, not yours — when you use Claude or GPT to draft a message, it sounds formal, structured, slightly over-explained. Second: the gap is especially obvious in short-form — Slack messages, emails, DMs. You can tell immediately when someone's AI wrote their message.
    - approach.heading: `"how it works"`
    - approach.body: Describe the flow — submit 10-20 real message samples, Tonos analyzes them across voice dimensions (formality, punctuation style, sentence length, directness, warmth), generates a structured voice profile. Then call `tonos_draft_message` or `tonos_rewrite_text` from any MCP-compatible client. Mention: RAG pipeline using Voyage AI embeddings for sample retrieval, credit system with atomic deduction before generation, SSE streaming via Hono.
    - approach.bullets: `["7 mcp tools — submit samples, generate profile, draft, rewrite, feedback", "voice dimensions: formality, punctuation, sentence length, directness, warmth, humor", "platform modes — linkedin auto-capitalizes even for casual writers", "bun + hono serving sub-5ms page renders, no react hydration overhead"]`
    - outcome.heading: `"what shipped"`
    - outcome.body: Tonos exposes 7 MCP tools that connect to Claude Code, Cursor, and any MCP client. Users submit samples, generate a profile, then draft or rewrite from inside their AI assistant. Live at tonos.fyi with Stripe billing, OAuth login, and an API key system for programmatic access.
    - tradeoffs.heading: `"decisions and tradeoffs"`
    - tradeoffs.body: Brief intro line about the two key architectural choices.
    - tradeoffs.bullets: `["rejected storing raw message history for rag — voice profile json with structured dimensions is portable, explainable, and doesn't grow unboundedly", "rejected react/next.js for the web app — server-rendered jsx via typed-htmx + hono means no hydration overhead, and the product is api-first anyway"]`
    - tech: Bun (runtime), Hono (framework), Anthropic SDK (service), PostgreSQL + pgvector (database), Voyage AI (service), Stripe (service), Zod (tool), MCP SDK (protocol)
    - links: `[{ label: "live", url: "https://tonos.fyi", type: "live" }, { label: "github", url: "https://github.com/mbeato/tonos", type: "github" }]`

    **Case Study 2: VTX** (slug: `vtx`)
    - title: `"vtx"`
    - hook: `"an athlete scoring engine that ranks training, social presence, and competition results into a single 0-1000 score for brand sponsorship matching"`
    - year: `"2025"`
    - status: `"live"`
    - problem.heading: `"the problem"`
    - problem.body: Two paragraphs. First: brands want to sponsor athletes who are actively training, posting consistently, and competing — not just athletes with big follower counts. Second: manually reviewing athlete profiles across Strava, Instagram, and competition results doesn't scale. There's no single metric that captures "this athlete is worth sponsoring right now."
    - approach.heading: `"how it works"`
    - approach.body: Describe the composite scoring algorithm — three independent signals (training via Strava webhooks, social via Instagram API, competition results), each scored on 90-day rolling windows (except competition which is all-time top-N). Equal weight average produces the VTX Score. Leaderboard snapshots every 15 minutes, daily archival. Distributed locking prevents duplicate calculations across server instances.
    - approach.bullets: `["three signals: training (strava webhooks, 90-day), social (instagram, 90-day, logarithmic), competition (all-time, top-n average)", "equal weight by design — changing weights later is a config change, not a data migration", "logarithmic scale on social audience — 100k engaged followers scores comparably to 10m passive followers", "nestjs + graphql code-first schema, postgresql via typeorm, redis cache, bull queues"]`
    - outcome.heading: `"what shipped"`
    - outcome.body: Multi-platform data aggregation feeding into a scoring engine with leaderboards, dashboards, and an Expo mobile app (iOS + Android). Athlete scores update within minutes of new training or social activity. Sponsor dashboard launching March 2026.
    - tradeoffs.heading: `"decisions and tradeoffs"`
    - tradeoffs.body: Brief intro about scoring design choices.
    - tradeoffs.bullets: `["equal weight (1/3 each) vs weighted scoring — rejected weighting because business priorities change. equal weights = config change later, not a data migration", "90-day rolling windows vs all-time — sponsors care about current activity, not lifetime stats. athletes can't coast on historical performance", "soft-delete for out-of-window posts — posts older than 90 days soft-deleted for storage, but historical score rebuilds include them so past scores stay accurate"]`
    - tech: NestJS (framework), GraphQL + Apollo (framework), PostgreSQL + TypeORM (database), Redis (database), Bull (tool), AWS Cognito (service), Expo (framework), Strava API (service)
    - links: `[{ label: "live", url: "https://vtxathlete.com", type: "live" }]` — NO GitHub link (private repo, per RESEARCH.md Pitfall 5)

    **Case Study 3: APIMesh** (slug: `apimesh`)
    - title: `"apimesh"`
    - hook: `"27 pay-per-call web analysis apis with crypto micropayments and an autonomous build loop that discovers demand, generates apis, and deploys without human involvement"`
    - year: `"2026"`
    - status: `"live"`
    - problem.heading: `"the problem"`
    - problem.body: Two paragraphs. First: AI agents can't use most APIs because they require signup flows, billing accounts, or OAuth — agents can't complete a signup form. Second: x402 makes API access as simple as an HTTP header. No accounts, no subscriptions. APIMesh built 27 focused web analysis APIs on this model.
    - approach.heading: `"how it works"`
    - approach.body: Describe the architecture — each API on its own subdomain (core-web-vitals.apimesh.xyz, security-headers.apimesh.xyz). Payment flow: agent hits endpoint, gets 402 with price/wallet/network, signs USDC payment on Base, includes X-PAYMENT header, Coinbase CDP verifies, server returns response. Free /preview endpoints on every API so agents can verify the response format before paying. 25-tool MCP server published to npm.
    - approach.bullets: `["27 apis on separate subdomains — independent deploy, independent rate limiting, no blast radius", "x402 payment flow: 402 response → usdc signature on base → coinbase cdp verification → response", "free /preview endpoints — agents verify response format before committing credits", "autonomous brain loop: monitor → scanner (sundays) → scout → build (up to 3 apis/run) → security audit → staging → prod", "brain loop security: external data sanitized, system-role prompt rules, 14+ vulnerability pattern audit on generated code"]`
    - outcome.heading: `"what shipped"`
    - outcome.body: 27 APIs live on subdomain routing, 25 MCP tools published to npm as @mbeato/apimesh-mcp-server, listed on Smithery and MCP Registry. The brain loop has autonomously built and deployed 9 APIs. Three payment methods: x402 USDC on Base, Stripe MPP, and API key auth.
    - tradeoffs.heading: `"decisions and tradeoffs"`
    - tradeoffs.body: Brief intro.
    - tradeoffs.bullets: `["rejected single monolithic api server — each api on its own subdomain for independent deploy and no blast radius from one broken api affecting others", "rejected requiring signup/api keys as default — x402 pay-per-call is the default because ai agents can't complete a signup flow", "free /preview on every api — rejected 'pay-to-discover' because agents need to confirm the api does what they need before committing credits", "security hardening on brain loop — rejected trusting llm output directly. generated code goes through a 14-pattern security audit before staging"]`
    - tech: Bun (runtime), Hono (framework), x402 (protocol), Coinbase CDP (service), Stripe MPP (service), Cheerio (tool), OpenAI (service), MCP SDK (protocol), SQLite (database), Caddy (tool)
    - links: `[{ label: "live", url: "https://apimesh.xyz", type: "live" }, { label: "github", url: "https://github.com/mbeato/APIMesh", type: "github" }, { label: "npm", url: "https://www.npmjs.com/package/@mbeato/apimesh-mcp-server", type: "npm" }]`

    **Case Study 4: awesome-mpp** (slug: `awesome-mpp`)
    - title: `"awesome-mpp"`
    - hook: `"a curated registry of 100+ machine payments protocol projects, sdks, and services — maintained autonomously by apimesh's brain loop"`
    - year: `"2026"`
    - status: `"open-source"`
    - problem.heading: `"the problem"`
    - problem.body: One paragraph. MPP launched in March 2026 — co-created by Stripe and Tempo Labs. Within weeks, 100+ SDKs, agent frameworks, and services appeared. Developers building in the space had no central directory.
    - approach.heading: `"how it works"`
    - approach.body: Awesome-list format on GitHub — lower maintenance burden than a database/website, GitHub stars and forks as community signal, pull request workflow for contributions. Maintained autonomously by APIMesh's brain loop scanner (runs every Sunday), which distinguishes it from a manually-curated list.
    - approach.bullets: `["15+ categories: protocol specs, sdks (7 languages), payment methods (15+ chains), framework integrations, infrastructure, agent tools", "autonomous maintenance — apimesh brain loop scanner runs weekly, discovers new projects, submits additions", "structured data in registry.json alongside the markdown for programmatic access"]`
    - outcome.heading: `"what shipped"`
    - outcome.body: 100+ projects catalogued across 15+ categories, covering 7 SDK languages and 15+ payment chains. The GitHub repo is the product — no separate website needed.
    - tradeoffs.heading: `"decisions and tradeoffs"`
    - tradeoffs.body: Brief intro.
    - tradeoffs.bullets: `["chose awesome-list format over a structured database/website — lower maintenance, github stars as community signal, pr workflow for contributions", "autonomous maintenance via brain loop vs manual curation — the scanner catches new projects faster than a human curator could", "the list explicitly links back to apimesh — these projects are related and cross-reference each other"]`
    - tech: Markdown (tool), GitHub Pages (service)
    - links: `[{ label: "github", url: "https://github.com/mbeato/awesome-mpp", type: "github" }]` — NO live URL (Pitfall 6: the repo IS the product)

    **Anti-slop gate (COPY-01):** Before finishing, verify NONE of these words appear in any case study body text: "innovative", "passionate", "cutting-edge", "robust", "scalable", "let's build something amazing", "available for opportunities", "modern stack". Use direct, specific language only.
  </action>
  <verify>
    <automated>cd /Users/vtx/max-portfolio && npm run build 2>&1 | tail -20 && echo "---SLOP CHECK---" && ! grep -riE "innovative|passionate developer|cutting.edge|robust solution|scalable architecture|modern stack|let.s build something" src/lib/case-studies.ts && echo "SLOP CHECK PASSED" && echo "---CONTENT CHECK---" && grep -c "slug:" src/lib/case-studies.ts && grep -cE "tonos|vtx|apimesh|awesome-mpp" src/lib/case-studies.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/case-studies.ts contains `slug: 'tonos'`
    - src/lib/case-studies.ts contains `slug: 'vtx'`
    - src/lib/case-studies.ts contains `slug: 'apimesh'`
    - src/lib/case-studies.ts contains `slug: 'awesome-mpp'`
    - src/lib/case-studies.ts contains `tonos.fyi`
    - src/lib/case-studies.ts contains `vtxathlete.com`
    - src/lib/case-studies.ts contains `apimesh.xyz`
    - src/lib/case-studies.ts contains `github.com/mbeato/awesome-mpp`
    - src/lib/case-studies.ts does NOT contain `github.com/mbeato/vtx` (private repo)
    - src/lib/case-studies.ts contains `Bun` and `Hono` (Tonos tech)
    - src/lib/case-studies.ts contains `NestJS` and `GraphQL` (VTX tech)
    - src/lib/case-studies.ts contains `x402` (APIMesh tech)
    - src/lib/case-studies.ts does NOT contain "innovative" or "passionate" or "cutting-edge" or "robust" or "scalable architecture"
    - Each case study object has `problem`, `approach`, `outcome`, and `tradeoffs` fields
    - Each `tradeoffs` field has `bullets` array with at least 2 items
    - npm run build exits with code 0
    - All 4 routes generate at build time (build output shows /work/tonos, /work/vtx, /work/apimesh, /work/awesome-mpp)
  </acceptance_criteria>
  <done>
    All 4 case studies populated with real content from 03-RESEARCH.md. Build passes. All 4 routes statically generated. No anti-slop violations in copy. Each case study has problem, approach, outcome, and tradeoffs with at least one rejected alternative.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. Build output lists /work/tonos, /work/vtx, /work/apimesh, /work/awesome-mpp as static pages
3. `npm run lint` passes
4. `grep -riE "innovative|passionate|cutting.edge|robust solution" src/lib/case-studies.ts src/app/work/ src/components/case-study/` returns no matches
5. `grep -c "tradeoffs" src/lib/case-studies.ts` returns 4+ (one per case study)
6. `grep "generateStaticParams" src/app/work/\[slug\]/page.tsx` returns a match
</verification>

<success_criteria>
- All 4 case study pages build and render
- Each page has: title, hook, tech tags, links, problem, approach, outcome, tradeoffs
- Tech stacks use actual library names (Bun, Hono, NestJS, x402, not "modern backend")
- Copy is in Max's voice — lowercase, direct, no buzzwords
- VTX has no GitHub link (private), awesome-mpp has no live URL (repo is the product)
- No anti-slop violations in any case study content
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-case-studies/03-01-SUMMARY.md`
</output>
