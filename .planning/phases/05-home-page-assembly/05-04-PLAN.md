---
phase: 05-home-page-assembly
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - SECT-01
  - SECT-02
  - SLOP-05

must_haves:
  truths:
    - "TopoSvgDivider renders at 3 section transitions (Hero->About, About->Experience, Experience->Projects)"
    - "No divider between Projects and Contact"
    - "Each section has a distinct scroll animation treatment visible during scroll"
    - "No uniform fade-in-on-scroll across all sections (SLOP-05)"
    - "User has approved visual appearance of all 5 sections with distinct animations and anti-slop compliance"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Page assembly with TopoSvgDivider placements between sections"
      contains: "TopoSvgDivider"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/dividers/TopoSvgDivider"
      via: "Import and render TopoSvgDivider component"
      pattern: "TopoSvgDivider"
    - from: "src/app/page.tsx"
      to: "src/lib/topo-paths"
      via: "Import DIVIDER_PATHS for each transition"
      pattern: "DIVIDER_PATHS"
---

<objective>
Wire TopoSvgDivider components into page.tsx at 3 section transitions, then verify the full page assembly with a visual checkpoint.

Purpose: Section dividers visually connect sections to the hero's topographic identity (SECT-01). The checkpoint confirms all 5 sections render correctly with distinct animations (SECT-02) and no anti-slop violations.
Output: page.tsx with 3 TopoSvgDivider placements. Full page verified visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/05-home-page-assembly/05-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/dividers/TopoSvgDivider.tsx:
```typescript
interface TopoSvgDividerProps {
  id: string
  paths: string[]
  width?: number
  height?: number
  className?: string
}
export default function TopoSvgDivider(props: TopoSvgDividerProps): JSX.Element
```

From src/lib/topo-paths.ts:
```typescript
export const DIVIDER_PATHS = {
  heroToAbout: string[]     // 3 paths
  aboutToExperience: string[]  // 2 paths
  experienceToProjects: string[]  // 3 paths
}
```

From src/app/page.tsx (current state):
```typescript
// These imports are currently commented out:
// import TopoSvgDivider from '@/components/dividers/TopoSvgDivider'
// import { DIVIDER_PATHS } from '@/lib/topo-paths'
```
</interfaces>

@.planning/05-home-page-assembly/05-01-SUMMARY.md
@.planning/05-home-page-assembly/05-02-SUMMARY.md
@.planning/05-home-page-assembly/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TopoSvgDivider into page.tsx at 3 section transitions</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current state -- has commented-out TopoSvgDivider import and comment about re-enabling)
    - src/components/dividers/TopoSvgDivider.tsx (component interface and props)
    - src/lib/topo-paths.ts (DIVIDER_PATHS object with 3 transition path sets)
    - .planning/05-home-page-assembly/05-UI-SPEC.md (TopoSvgDivider Placement section -- exactly which transitions)
  </read_first>
  <action>
    Uncomment and wire TopoSvgDivider into page.tsx at the 3 specified transitions.

    1. Uncomment the two import lines (remove the `//` and the comment above them):
    ```typescript
    import TopoSvgDivider from '@/components/dividers/TopoSvgDivider'
    import { DIVIDER_PATHS } from '@/lib/topo-paths'
    ```

    2. Remove the comment `// TopoSvgDivider and DIVIDER_PATHS available -- re-enable when ready`

    3. Add TopoSvgDivider components between sections in the JSX. The page structure becomes:
    ```jsx
    <main id="portfolio-main-page" className="relative">
      <Navigation id="portfolio-navigation" />
      <Hero id="portfolio-hero-section" />
      <TopoSvgDivider id="divider-hero-to-about" paths={DIVIDER_PATHS.heroToAbout} />
      <About id="portfolio-about-section" />
      <TopoSvgDivider id="divider-about-to-experience" paths={DIVIDER_PATHS.aboutToExperience} />
      <Experience id="portfolio-experience-section" />
      <TopoSvgDivider id="divider-experience-to-projects" paths={DIVIDER_PATHS.experienceToProjects} />
      <Projects id="portfolio-projects-section" />
      <Contact id="portfolio-contact-section" />
      <AccessibilityMenu id="portfolio-accessibility-menu" />
    </main>
    ```

    Key points:
    - 3 dividers total: Hero to About, About to Experience, Experience to Projects
    - NO divider between Projects and Contact (per UI-SPEC decision)
    - Each divider gets a unique id string for the SVG element
    - paths prop receives the corresponding DIVIDER_PATHS entry
    - page.tsx is a server component -- TopoSvgDivider is a client component with 'use client' directive, so the import is valid (Next.js handles the boundary)
    - DIVIDER_PATHS is imported from topo-paths.ts (pure data file, no client boundary issues -- per Phase 4 decision)
  </action>
  <verify>
    <automated>cd /Users/vtx/max-portfolio && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `import TopoSvgDivider from '@/components/dividers/TopoSvgDivider'` (uncommented)
    - page.tsx contains `import { DIVIDER_PATHS } from '@/lib/topo-paths'` (uncommented)
    - page.tsx does NOT contain the comment "TopoSvgDivider and DIVIDER_PATHS available"
    - page.tsx contains `<TopoSvgDivider id="divider-hero-to-about" paths={DIVIDER_PATHS.heroToAbout}`
    - page.tsx contains `<TopoSvgDivider id="divider-about-to-experience" paths={DIVIDER_PATHS.aboutToExperience}`
    - page.tsx contains `<TopoSvgDivider id="divider-experience-to-projects" paths={DIVIDER_PATHS.experienceToProjects}`
    - page.tsx does NOT contain a TopoSvgDivider between Projects and Contact
    - page.tsx contains exactly 3 `<TopoSvgDivider` elements
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>page.tsx has TopoSvgDivider wired at 3 section transitions with draw-on path animation triggered by scroll intersection. No divider between Projects and Contact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of full home page assembly</name>
  <files>src/app/page.tsx</files>
  <action>
    This is a visual verification checkpoint. The executor starts the dev server (`npm run dev`) and presents the verification checklist below to the user. No code changes in this task.

    Verification checklist for the user:

    1. Hero: Complete/skip puzzle. Subtitle "purdue cs + 4 years building systems that run at inference speed" appears below name.
    2. About: 4 bio paragraphs visible, 4 photos render (Me.jpg prominent), caption in mono font, text slides left / photos fade in.
    3. Experience: 3 entries (VertikalX, DocReserve, Data Mine), coral left border on hover, mono tech tags, entries slide in from left.
    4. Projects: 4 cards stacked vertically (NOT grid), each shows title/year/hook/tags/"view case study" CTA, click navigates to /work/[slug].
    5. Contact: Heading "say hi", no timezone/demo notice, shadow-border inputs with coral focus, coral submit button, resume download link in left column.
    6. Dividers: TopoSvgDivider lines draw on scroll at 3 transitions, no divider before Contact.
    7. Navigation: Resume button is outlined/secondary, clicking triggers PDF download.
    8. Anti-slop: No bento grid, no skill bars, no logo grid, no generic CTAs, different animation per section, all copy lowercase.
  </action>
  <verify>
    <automated>cd /Users/vtx/max-portfolio && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - User approves visual appearance of all 5 sections
    - User confirms distinct scroll animation per section
    - User confirms anti-slop checklist passes
    - User confirms resume download works from both navigation and contact section
    - User confirms project card links navigate to /work/[slug]
  </acceptance_criteria>
  <done>User has visually verified the complete home page assembly. All sections render correctly, animations are distinct, anti-slop checklist passes, and navigation works.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- 3 TopoSvgDivider elements render in page.tsx
- All 5 sections visible with distinct animations
- Anti-slop checklist passes on visual inspection
</verification>

<success_criteria>
1. TopoSvgDivider draws contour lines at 3 section transitions during scroll
2. All sections render with correct content and styling
3. Each section has a visually distinct scroll animation
4. Anti-slop checklist passes (no bento grid, no skill bars, no generic CTAs, no uniform animations)
5. Recruiter can reach a case study within 2 scrolls + 1 click from hero
6. Resume download works from both navigation and contact section
7. User approves the visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/05-home-page-assembly/05-04-SUMMARY.md`
</output>
